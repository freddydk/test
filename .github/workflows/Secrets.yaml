name: Secrets

on:
  workflow_dispatch:

defaults:
  run:
    shell: PowerShell

jobs:
  update:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: GetSecret
        run: |
          $testSecret = '${{ secrets.TESTREPOSECRET }}'
          Write-Host $testSecret
          Write-Host $testSecret.Length
          Write-Host "BönæøåÆØÅ"
          Write-Host "BönæøåÆØÅ".Length

          Write-Host ($testSecret -eq "BönæøåÆØÅ")

          Write-Host "{ test }"

      - name: GetSecret
        run: |
          $testSecret = '${{ secrets.TESTREPOSECRET2 }}'
          Write-Host $testSecret
