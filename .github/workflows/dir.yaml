name: Dir

on:
  workflow_dispatch:

defaults:
  run:
    shell: PowerShell

jobs:
  update:
    runs-on: windows-latest

    steps:
      - name: GCI
        run: |
          Set-location "c:\"
          Get-ChildItem
          $ENV:TESTENV = "FREDDY"

      - name: GCI2
        run: |
          Write-Host "ENVVAR $ENV:TESTENV"
