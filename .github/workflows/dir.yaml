name: Dir

on:
  workflow_dispatch:

defaults:
  run:
    shell: PowerShell

jobs:
  update:
    runs-on: windows-latest

    steps:
      - name: GCI
        run: |
          Set-location "c:\"
          Get-ChildItem -Recurse
          $ENV:TESTENV = "FREDDY"

      - name: GCI
        run: |
          Write-Host "ENVVAR $ENV:TESTENV"
